name: storageservice-tests

on:
  push:
    branches: [ "**" ]
    paths:
      - 'kajz/storage-service/src/**'
      - 'kajz/storage-service/pom.xml'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'kajz/storage-service/src/**'
      - 'kajz/storage-service/pom.xml'

jobs:
  run-tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: kajz/storage-service
    steps:
    - uses: actions/checkout@v6
    - name: Set up JDK 25
      uses: actions/setup-java@v5
      with:
        java-version: '25'
        distribution: 'temurin'
        cache: maven
        cache-dependency-path: 'kajz/storage-service/pom.xml'
    - name: Build and Test with Maven
      run: mvn -B clean verify -Dspring.profiles.active=test -Denable.tc=true
      env:
        TESTCONTAINERS_RYUK_DISABLED: false
        ENABLE_TC: true # to enable/disable testcontainer tests
